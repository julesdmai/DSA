/**
 * @param {string} s
 * @return {number}
 */

// Strategy: Two stacks to hold numbers and operations, first pass to handle mult/div and second pass to handle add/sub
// O(n) // spaceO(n)
// Input: String
// Output: Number
var calculate = function(s) {
    
};